<section class="basic-page">
  <div class="container">
    <h1>{{ article.title }}</h1>
    <p>{{ article.published_at | date: '%d %b %Y' }}</p>
    <div class="basic-page__content">{{ article.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Article (baseline)", "settings": [] }
{% endschema %}
